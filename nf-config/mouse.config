params {
  species         = "Mouse"
  bowtie_index    = "$baseDir/data/bowtie/mouse/mouse_index"
  // multi-FASTA backing the index above; used by the optional --split step
  // to annotate each position with its nucleotide.
  reference_fasta = "$baseDir/data/bowtie/mouse/Mouse.fasta"
}
